const SubTitleCheckOut = (props: any) => {
  const { title } = props;
  return (
    <h1 className="text-text text-[1.6em] font-semibold mb-[20px]">{title}</h1>
  );
};

export default SubTitleCheckOut;
